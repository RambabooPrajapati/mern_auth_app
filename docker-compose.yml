version: '3'
services:
  client:
    build: ./client
    ports:
      - "3000:4173"
    depends_on:
      - server

  server:
    build: ./server
    ports:
      - "5000:5000"
    env_file:
      - ./server/.env
