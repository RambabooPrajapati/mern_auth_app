version: '3'
services:
  frontend:
    build: ./frontend
    env_file:
      - ./frontend/.env
    ports:
      - "3000:4173"
    depends_on:
      - backend

  backend:
    build: ./backend
    ports:
      - "5000:5000"
    env_file:
      - ./backend/.env
